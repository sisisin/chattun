## Round 1

STATUS: NEEDS_FIX

### 指摘事項

1. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:100]**: `<a>` タグの `rel` 属性が `rel="noopener"` のみで `noreferrer` が欠けている。同ファイル内の他の外部リンク（例: `titleLink` のアンカータグ）は `rel="noopener noreferrer"` を使用しており、統一が必要。同じ問題が `TweetContent` の `message.files.map` （141行目）でも存在するが、それは既存コードのため今回のスコープ外として、新規追加分（100行目）を修正すること。

   ```tsx
   // NG
   <a key={i} href={file.urlPrivate} target="_blank" rel="noopener">
   
   // OK
   <a key={i} href={file.urlPrivate} target="_blank" rel="noopener noreferrer">
   ```

2. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:101]**: 画像 `<img>` に `alt` 属性がない。アクセシビリティ上、`alt=""` を明示する必要がある（装飾的画像として扱う場合でも空文字を明示する）。同ファイル内の他の画像要素（`att.imageUrl`、`tweetAttachmentAuthorIcon`）はすべて `alt=""` を持っている。

   ```tsx
   // NG
   <img className={styles.tweetContentsImage} src={`${basePath}/api/file?${params}`} />
   
   // OK
   <img className={styles.tweetContentsImage} src={`${basePath}/api/file?${params}`} alt="" />
   ```

3. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:96]**: `att.files?.map((file, i) => ...)` でインデックス `i` をキーとして使用している。同一コンポーネント内の他の `.map` 呼び出しも同様のパターンを使っており慣習として存在するが、リストが不変でない場合は警告が出る可能性がある。既存コードと一貫性があるため許容範囲内だが、念のため確認。  
   **→ 軽微のため SHOULD レベル（必須ではない）**

4. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:98]**: `thumb360` をプロキシ経由で取得する際の URL が相対パスになりうる点を確認すること。モックプリセット（`presets.ts`）では `thumb_360` に `/api/mock/assets/sample.png` という相対パスが入っているが、これを `target_url` としてそのまま `/api/file?target_url=/api/mock/assets/sample.png` に渡すと、サーバー側でフェッチする際に壊れる可能性がある。実際のSlackデータでは絶対URLが来るため本番影響はないが、モックでの動作確認が意図通りになるか確認すること。  
   **→ モック固有の問題のため SHOULD レベル（必須ではない）**

### 必須修正まとめ

- 指摘1: `rel="noopener noreferrer"` に修正（セキュリティ規約）
- 指摘2: `alt=""` を追加（アクセシビリティ）

## Browser Verification

STATUS: ISSUES_FOUND

### 確認内容

- **long-attachment の折りたたみ**: OK - 長いattachment（60件のPRリスト）が一定の高さで折りたたまれ、「Show more」ボタンが表示される。クリックすると全内容が展開され、「Show less」ボタンで再度折りたためる。トグル動作は正常。
- **attachment-with-image の画像表示**: 画像の `<img>` 要素はattachment内に存在するが、`/api/file` プロキシ経由のためモック環境では画像ロードに失敗している（naturalWidth=0, naturalHeight=0）。表示サイズが16x16pxの壊れた画像アイコンとなっている。HTML構造自体（`<a>` でラップされた `<img class="tweetContentsImage">`）は正しく生成されている。
- **コンソールエラー**: なし
- **mrkdwn リンクのパース**: attachment内のテキスト部分の `<url|label>` 形式のリンクは正しくパースされリンクとして表示される。ただしフッター部分（`footer` フィールド）は `<https://github.com/example/repo|example/repo>` がmrkdwn形式のまま生テキストとして表示されている。

### 問題点

1. **attachment内画像がモック環境で表示されない**: attachment内の画像が `/api/file?target_url=...` プロキシ経由で取得されるが、モックサーバーにはこのエンドポイントがないため404になる。画像が読み込めない場合のサイズが16x16pxと非常に小さく、壊れた画像であることが視覚的に分かりにくい。本番環境（Slack APIが `/api/file` で中継する）では正常に表示される見込みだが、モックでの検証はできなかった。

2. **フッターのmrkdwnがパースされていない**: `long-attachment` のフッター `<https://github.com/example/repo|example/repo>` がSlack mrkdwn形式のまま表示されている。テキスト本体はmrkdwnパースされているが、フッターフィールドには適用されていない。

3. **モックサーバーのプリセット未反映**: `long-attachment` と `attachment-with-image` プリセットがpresets.tsに定義されているが、起動中のモックサーバーには反映されていない（再起動が必要）。検証はraw APIで代替した。

## Round 2

STATUS: NEEDS_FIX

### Round 1指摘の対応確認

- 指摘1（`rel="noopener noreferrer"`）: 対応済み — TweetContent.tsx 100行目で `rel="noopener noreferrer"` に修正されている
- 指摘2（`alt=""` 追加）: 対応済み — TweetContent.tsx 103行目で `alt=""` が追加されている

### 指摘事項

1. **[front/src/app/features/timeline/components/tweet/components/Tweet.module.css]**: `styles.tweetAttachmentBody` が参照されているが、対応するCSSクラス `.tweet-attachment-body` が定義されていない。TweetContent.tsx の68〜72行目で `styles.tweetAttachmentBody` を使用しているが、このクラスはCSS Modulesに存在しないため `undefined` となり、折りたたみ時に `className="undefined tweet-attachment-body-collapsed"` という不正なクラス名がレンダリングされる。展開時は `className={undefined}` となりスタイルが何も当たらない。

   ```css
   /* 追加が必要 */
   .tweet-attachment-body {
     /* 折りたたみ/展開の共通スタイルがあれば記述 */
   }
   ```

   または、クラスが不要であれば TSX 側を修正する:

   ```tsx
   // isOverflowing && !isExpanded のときだけ collapsed クラスを付与する
   className={isOverflowing && !isExpanded ? styles.tweetAttachmentBodyCollapsed : undefined}
   ```

## Browser Verification Round 2

STATUS: OK

### 確認内容

- **long-attachment の折りたたみ**: OK — 長いattachment（60件のPRリスト）が一定の高さで折りたたまれ、「Show more」ボタンが表示される。クリックすると全内容（#1000〜#1059）が展開され、末尾に「Show less」ボタンが表示される。「Show less」クリックで再度折りたたまれ「Show more」に戻る。トグル動作は正常。
- **attachment-with-image の画像表示**: OK — attachment内に画像が正しく表示されている。著者名「Mock User」、テキスト「これは画像付きの引用メッセージです」、緑色のサンプル画像、フッター「Thread in Slack Conversation」が全て表示。画像は `/api/file?target_url=%2Fapi%2Fmock%2Fassets%2Fsample.png` 経由で取得されており、プロキシ経由の画像表示が正常に動作している。
- **message-with-image の既存機能**: OK — 「画像付きメッセージ」テキストと緑色のサンプル画像が正常に表示される。既存の画像付きメッセージ表示機能に回帰はない。
- **コンソールエラー**: なし — ページロード後、3つのプリセットイベント受信後ともにコンソールエラーは発生していない。
- **レイアウト・デザイン**: attachmentの左ボーダー、テキストの折りたたみ表示、画像のサイズ感は適切。「Show more」/「Show less」ボタンは視認しやすい位置に配置されている。

## Round 3

STATUS: APPROVED

Round 2の指摘（`styles.tweetAttachmentBody` 未定義）が正しく対応された。TSX側を `isOverflowing && !isExpanded ? styles.tweetAttachmentBodyCollapsed : undefined` に修正し、`.tweet-attachment-body-collapsed` と `.tweet-attachment-toggle` のCSSクラスも適切に定義されている。セマンティックトークン（`var(--spacing-xs)`、`var(--font-small-ex)`、`var(--button-primary)`）も正しく使用されており、`autoprefixer` による `-webkit-mask-image` の自動付与も設定済みのため、Safariでの折りたたみグラデーションも問題ない。タスクの達成条件（長いattachmentの折りたたみ、attachment内の画像表示）はいずれも実装されており、コード品質・規約準拠・正確性の観点で指摘事項はない。
