## Round 1

STATUS: NEEDS_FIX

### 指摘事項

1. **[server/mock-server/presets.ts:179-192]**: `message-with-emojis` プリセットで「標準絵文字」と「カスタム絵文字」の両行に同じ `:thumbsup:` が使われており、「カスタム絵文字」行を区別するための実質的な意味がない。`message-with-emojis` の目的は絵文字構文のデモであるため内容として機能はしているが、コメントとして「カスタム絵文字」と書いておきながら標準絵文字と同じ名前を使っているのは紛らわしい。カスタム絵文字のつもりであれば `:custom_emoji:` や `:my_app_icon:` のような、標準絵文字と区別できる名前にするか、「カスタム絵文字」の記述を削除して絵文字一覧のデモに統一することを推奨する。

2. **[server/mock-server/presets.ts:149-162]**: パーサー（`parser.ts`）が対応している `group_mention` ノードタイプ（`<!subteam^GROUPID|@name>` 形式）が `message-with-mentions` プリセットに含まれていない。タスクの達成条件「mrkdwnパーサーが対応している全構文をカバーするプリセットが存在すること」に照らすと、`group_mention` 構文もカバーされる必要がある。`message-with-mentions` のテキストに `<!subteam^S012345|@dev-team>` のような記述を追加することを推奨する。

## Round 2

STATUS: NEEDS_FIX

### Round 1 指摘への対応確認

1. **指摘1（message-with-emojis）**: 「標準絵文字」行を `:wave: :fire: :rocket:` に変更し、`:thumbsup:` の重複は解消された。ただし「カスタム絵文字」行に `:thumbsup: :heart: :laughing:` が残っており、これらはいずれも広く知られた標準絵文字名であるため、「カスタム絵文字」というラベルが依然として不正確。Round 1 の推奨（`:custom_emoji:` のような明確にカスタムと分かる名前を使うか、ラベルを削除する）が完全には実施されていない。

2. **指摘2（group_mention）**: `<!subteam^S12345|@mock-team>` が `message-with-mentions` に追加された。対応済み。

### 指摘事項

1. **[server/mock-server/presets.ts:186]**: `message-with-emojis` の「カスタム絵文字」行に `:thumbsup: :heart: :laughing:` が使われているが、これらはいずれも標準絵文字として認識される名前であり「カスタム絵文字」というラベルと一致しない。`:custom_emoji_name:` や `:my_org_icon:` のような明らかにカスタムと分かる名前に変更するか、行のラベルを「その他の絵文字」等に修正することを推奨する。

### Dismissed

- **指摘1**: `thumbsup`, `heart`, `laughing` はモックサーバーのブートストラップデータ（`index.ts:30-35`）でカスタム絵文字URLが定義されているため、フロントエンドのMrkdwnRenderer.tsxでは `resolveEmoji` がURLを返し、実際にカスタム絵文字（`<img>` タグ）として表示される。名前が標準絵文字と同じでも、モック環境ではカスタム絵文字として正しく機能している。

## Round 3

STATUS: APPROVED

Round 2 の残り指摘（`thumbsup`/`heart`/`laughing` がカスタム絵文字として機能するかどうか）は dismiss が妥当であると判断する。`server/mock-server/index.ts:30-35` でこれら3つの名前にカスタム絵文字URL（`emoji.slack-edge.com/...`）が定義されており、`MrkdwnRenderer.tsx` の `resolveEmoji` がURLを返すため `<img>` タグとして描画される。テキスト上の「カスタム絵文字」ラベルはモックデータの説明として機能的に正確であり、新たに指摘する理由はない。

タスク達成条件はすべて満たされている。パーサーが対応する全ノードタイプ（`bold`/`italic`/`strike`/`code`/`codeblock`/`linebreak`/`user_mention`/`channel_ref`/`group_mention`/`special_mention`/`emoji`/`link`）が各プリセットによってカバーされており、`message-with-image` への `filetype` 追加も完了、既存プリセットへの影響もない。
