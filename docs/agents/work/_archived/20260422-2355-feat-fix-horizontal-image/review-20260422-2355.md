## Round 1

STATUS: NEEDS_FIX

### 指摘事項

1. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:141-143]**: `message.files` をレンダリングしている `<a>` タグに `tweetContentsImage` クラスが適用されているのは `<img>` 要素だけで、`<a>` タグ自体はインライン要素のままです。`display: block` を `<img>` に付与しても、その親の `<a>` がインライン要素であるため、テキスト直後に `<a>` が横に並ぶ問題は解決しません。タスクのスコープに「`<a><img></a>` が常に縦に並ぶこと」「テキストと画像の間も改行されること」と明記されているとおり、`<a>` タグ自体を `display: block` にする必要があります。

   具体的には、`.tweet-contents-image` を `<img>` ではなく `<a>` 要素に適用するか、`<a>` に別途 `display: block` のスタイルを当てる対応が必要です。

   `TweetContent.tsx` の `message.files` レンダリング（141行目の `<a>`）、および `AttachmentView` の `att.files` レンダリング（96行目の `<a>`）の両方が同様の問題を持っています。

2. **[front/src/app/features/timeline/components/tweet/components/TweetContent.tsx:125-127]**: タスクのスコープに「BlockKitContentのrenderでspan要素で出力されるリッチテキストが画像と横並びにならないようにする」と記載されていますが、この対応がなされていません。`BlockKitContent` が返す `<span>` がインライン要素のまま画像と横並びになるケースへの対処が必要かどうか確認・対応が必要です。タスク達成条件2「テキストと画像の間も改行されること」に関係するため、明示的に対処したか否かをコメントとして確認してください。

## Browser Verification

STATUS: OK

### 確認内容

- **画像付きメッセージの表示**: `message-with-image` プリセットを送信し、テキスト「画像付きメッセージ」の下に画像が縦に表示されることを確認した（テキスト bottom=108px, 画像 top=112px）
- **`display: block` の適用**: `.tweet-contents-image` クラス（CSSモジュールにより `_tweet-contents-image_ga4q9_119` に変換）の `<img>` 要素に `display: block` が正しく適用されている
- **画像サイズ**: 360x240px で表示され、コンテナ幅（2136px）内に収まっている。横にはみ出す問題は発生していない
- **通常メッセージの回帰確認**: `simple-message` プリセット（テキストのみ）が正常に表示されている（height=112px、画像要素なし）
- **コンソールエラー**: なし

### 備考

- Round 1 のコードレビュー指摘事項1（`<a>` タグ自体がインライン要素のまま）について、現在のブラウザ表示では画像がテキストの下に正しく縦に配置されている。ただし、これは現在のモックデータ（画像1枚）での確認であり、複数画像が存在する場合や特定のコンテンツ幅の条件下では `<a>` がインラインのままでレイアウト崩れが発生する可能性がある
- attachment の画像表示については、対応するモックプリセットがないため未検証

### Dismissed

- 指摘2: BlockKitContentのspan対応について。`<img>` に `display: block` を適用することで、ブロック要素としてレンダリングされるため、直前のインライン要素（`<span>`）とは自然に改行される。ブラウザ検証でもテキストと画像が正しく縦に分離されていることを確認済み（テキスト bottom=108px, 画像 top=112px）。追加のspan対応は不要。

## Round 2

STATUS: APPROVED

Round 1 の指摘事項がすべて適切に修正されている。`<a>` タグへの `tweetContentsImageLink` クラス付与（`display: block`）が `TweetContent` の `message.files` と `AttachmentView` の `att.files` の両方に適用済み。また `message.imageAttachments` にも `tweetContentsImage` クラスが追加され、一貫したブロック表示が実現している。Round 1 の dismiss（指摘2）も CSS の仕様上妥当であり受け入れる。
