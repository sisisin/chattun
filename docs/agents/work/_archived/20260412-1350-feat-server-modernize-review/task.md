# Task: serverでモダナイズが必要な点がないかをレビューし、Taskとして追加する

## 背景

front側のモダナイズが一段落し、server側もvp check導入が完了した。次のステップとして、serverのコード・依存関係・設定を全体的にレビューし、モダナイズが必要な点を洗い出す。

## 達成条件

1. server/src/ 配下の全ソースコードをレビューする
2. server/package.json の依存関係をレビューする（バージョンの古さ、不要な依存、型定義パッケージの整理）
3. server の設定ファイル（tsconfig.json等）をレビューする
4. 発見した改善点をTODO.mdにTaskとして追加する

## スコープ

- server/ 配下のコード・設定・依存関係のレビュー
- TODO.mdへのTask追記
- コード変更は行わない（レビューと計画のみ）
