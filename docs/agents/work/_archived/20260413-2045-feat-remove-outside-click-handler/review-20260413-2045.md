## Round 1

STATUS: APPROVED

`react-outside-click-handler` の依存が package.json・型定義ともに削除され、`useRef<HTMLDivElement>` + `document.addEventListener('mousedown', ...)` による自前実装に正しく置き換えられている。`isEmojiMenuVisible` が `false` のときはエフェクト内で早期 `return` するためリスナーは登録されず、クリーンアップ関数も両リスナーを確実に除去しており、メモリリークの懸念もない。タスクの達成条件（パッケージ削除・外側クリックで閉じる動作の維持）をいずれも満たしている。

## Browser Verification

STATUS: OK

### 確認内容

- 絵文字ピッカーの開閉（リアクション追加アイコンクリック）: OK。クリックで正常に絵文字ピッカーが表示される
- 外側クリックで閉じる: OK。ピッカー外の領域をクリックすると即座に閉じる
- Escapeキーで閉じる: OK。Escapeキー押下で即座に閉じる
- 絵文字選択でリアクション追加: OK。thumbs up を選択し、メッセージにリアクション（1）が表示された。選択後にピッカーも自動で閉じた
- コンソールエラー: なし
- 表示崩れ: なし。ヘッダー、メッセージ、リアクションバッジ、アクションアイコンすべて正常に表示
