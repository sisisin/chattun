## Round 1

STATUS: NEEDS_FIX

### 指摘事項

1. **[front/package.json:10]**: `babel-runtime: "6.26.0"` が `dependencies` に残存している。これは CRA + Babel 環境向けのランタイムであり、Vite 移行後は不要。削除すること。

2. **[README.md:22]**: `create-react-app の HTTPS 利用の機能` への参照が残っている。CRA は削除されたため、この記述は実態と乖離している。Vite での HTTPS 設定方法（`vite.config.ts` の `server.https`）に更新するか、記述を除去すること。

3. **[README.md:28]**: `cra が提供してくれるやつをそのまま使うだけの方法` という記述が残っている。CRA 削除後は意味をなさないため、この節の内容を Vite に即した説明に更新すること。

4. **[README.md:49-51]**: `HOST`, `SSL_CRT_FILE`, `SSL_KEY_FILE` 環境変数はすべて CRA 固有のもの。Vite では `server.https.cert` / `server.https.key` でTLS証明書を設定する。フロント側の環境変数説明を Vite 向けに更新すること。

5. **[README.md:96]**: `REACT_APP_BASE_URL` の記述が残っている。`front/src/app/config.ts` では既に `VITE_BASE_URL` に移行済みであるため、ドキュメントも `VITE_BASE_URL` に揃えること。

6. **[front/vite.config.ts:16-25]**: `server.https` の設定が存在しない。元々 `front/.env.development` に `HTTPS=true` が設定されており CRA の HTTPS 機能を利用していた。mock-dev スキルのドキュメントでも `https://local.sisisin.house:3000/` でのアクセスを前提としている。Vite で HTTPS を有効にするには `server.https` の設定が必要であり、現状の設定では HTTPS が動作しない可能性がある。実際に `https://local.sisisin.house:3000/` でアクセスできるかを確認し、必要であれば `server.https` を設定すること。

### Dismissed

1. `babel-runtime`はCRA/Babel固有ではなく、emoji-mart 2.11.2の実行時依存。TODO.mdに「frontの暗黙的な推移的依存を整理する」タスクで管理済み。

## Round 2

STATUS: NEEDS_FIX

### 指摘事項

1. **[front/vite.config.ts:16-17,34 / README.md:49-51]**: README に記載されている環境変数名と `vite.config.ts` が実際に読み取る変数名が一致していない。README では `VITE_DEV_HOST`、`VITE_DEV_SSL_CERT`、`VITE_DEV_SSL_KEY` と説明しているが、`vite.config.ts` では `HOST`、`SSL_CRT_FILE`、`SSL_KEY_FILE` を参照している。どちらかに統一すること。なお Vite の `loadEnv` などを使わずに手動で `.env.local` をパースしているため、`VITE_` プレフィックスは必須ではないが、ドキュメントとコードを一致させることは必須。

### Dismissed の再評価

Round 1 の Dismissed 1（`babel-runtime`）：`yarn.lock` を確認したところ `babel-runtime` を推移的依存として参照しているパッケージは存在せず、`emoji-mart 2.11.2` の依存でもない（`emoji-mart` の依存は `prop-types` のみ）。そのため「emoji-martの実行時依存」という説明は不正確。ただし「TODO.mdで別タスクとして管理済み」という部分は有効な理由であるため、このブランチのスコープ外として dismissal 自体は受け入れる。

## Round 3

STATUS: APPROVED

Round 2 の指摘事項（README の環境変数名 `VITE_DEV_HOST` 等と `vite.config.ts` のコードの不一致）について、README を `HOST`、`SSL_CRT_FILE`、`SSL_KEY_FILE` に揃える形で修正され、コードとドキュメントが一致していることを確認した。CRA 関連の依存（`react-scripts`、`@craco/craco`、`@babel/core`、`babel-loader`、`http-proxy-middleware`、`@types/webpack-env`）の削除、環境変数の `REACT_APP_*` → `VITE_*` 移行、`import.meta.env` / `import.meta.hot` への置き換えも適切に実施されており、移行として問題ない状態になっている。
