## Round 1

STATUS: APPROVED

タスクの達成条件を満たしている。`inThread` prop を `Tweet` → `TweetView` と正しくパススルーし、`ThreadView` から `inThread={true}` を渡すことでスレッド画面での「スレッドを表示」リンクを非表示にしている。タイムライン画面では `inThread` が未指定（falsy）のため既存の表示動作が維持される。変更は最小限でシンプルであり、feature モジュールパターンにも準拠している。

## Browser Verification

STATUS: OK

### 確認内容

- タイムライン画面（/）でスレッド付きメッセージ（thread_ts を持つメッセージ）に「スレッドを表示」テキストリンクが正しく表示されることを確認した
- スレッド画面（/thread/CMOCKCH001/:ts）では「スレッドを表示」テキストリンクが表示されないことを確認した（アクセシビリティツリーでもリンク要素が存在しないことを検証済み）
- スレッド画面で親メッセージとスレッド返信の両方が正常に表示されていることを確認した
- リアクション（:thumbsup:）がリアルタイムで正常に表示されることを確認した
- スレッドアイコンリンク、チャンネルリンク（#mock-general）、Slackリンクがタイムライン・スレッド両画面で正常に存在することを確認した
- 「戻る」リンクによるスレッド画面からタイムライン画面への遷移が正常に動作することを確認した
- コンソールエラーなし
- 表示崩れなし（レイアウト、要素の配置は適切）
