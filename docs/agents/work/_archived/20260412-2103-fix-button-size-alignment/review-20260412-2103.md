## Round 1

STATUS: APPROVED

`.button-add` から `font-size: var(--font-small)` と `margin-top: var(--spacing-xs)` を削除することで、`.button-primary` と同じフォントサイズ・paddingになり、タスクの達成条件（同じ高さで表示・フォントサイズとpaddingの統一）を満たしている。変更範囲はスコープ通り `_form.css` の `.button-add` のみで、他への影響もない。

## Browser Verification

STATUS: OK

### 確認内容

- 設定画面（/setting）を開き、Channel Matchセクションで「+ 条件を追加」ボタンをクリックして条件行を追加。「+ 条件を追加」ボタンと「保存」ボタンが横並びで表示されることを確認
- ミュートセクションで「+ ミュートするユーザーを追加」ボタンをクリックして条件行を追加。同様に「+ ミュートするユーザーを追加」ボタンと「保存」ボタンが横並びで表示されることを確認
- 両セクションとも、button-addとbutton-primaryのfontSize(16px)、lineHeight(24px)、padding(8px 16px)が一致していることをcomputed styleで確認
- button-addの高さは42px（1px dashed border込み）、button-primaryの高さは40px（border無し）で、borderを除いた内部コンテンツサイズは同一（padding 8px + lineHeight 24px = 40px）
- marginTopは両方とも0pxで、以前あった余計なmargin-topは除去されている
- コンソールエラーなし
- 表示崩れなし
