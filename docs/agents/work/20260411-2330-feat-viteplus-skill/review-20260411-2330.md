## Round 1

STATUS: NEEDS_FIX

### 指摘事項

1. **[.claude/skills/vite-plus/SKILL.md:4]**: `user-invocable: false` というフロントマターフィールドが他の既存スキル（`mock-dev`, `shell-scripting`, `do-task`）には存在しない。`skill-dev` スキルは仕様確認に `claude-code-guide` エージェントを使うよう指示しているが、`user-invocable` が有効なフィールドである確認が取れていない。もし無効なフィールドであれば、意図した「背景知識として自動参照」の動作が得られない。フィールドの有効性を確認し、不要であれば削除すること。

2. **[.claude/skills/vite-plus/SKILL.md:49]**: `import { defineConfig } from 'vite-plus'` のパッケージ名が不正確な可能性がある。Vite+ の実際のパッケージ名は `vite` である可能性が高く（Vite+ は CLI ツール名であってnpmパッケージ名ではない）、誤ったインポートパスをリファレンスとして記載するとコード生成時に誤りを引き起こす。実際のパッケージ名・インポートパスを確認して修正すること。

3. **[.claude/skills/vite-plus/SKILL.md:35]**: `vp test watch` というコマンド形式が疑わしい。Vitest ベースの CLI では通常 `vp test --watch` のようにフラグ形式を使う。同ファイル内の36行目 `vp test run --coverage` は `run` サブコマンドを使っているのに対し、`watch` だけサブコマンド形式になっており一貫性がない。実際のコマンド形式を確認して統一すること。

4. **[.claude/skills/vite-plus/SKILL.md:40]**: `vp env` コマンドの説明が「Node.js バージョン管理」となっているが、これは Vite+ の本来の機能範囲（Web ツールチェイン）から外れる。`vp env` が実際に存在するコマンドか、またその機能が正しいかを確認すること。誤った情報であれば削除すること。

5. **[.claude/skills/vite-plus/SKILL.md:全体]**: このファイルは Vite+ のリファレンスとして作成されているが、Vite+ は 2025年8月時点では安定版が未リリースまたは開発中の段階であり、記載されている情報（CLIコマンド、設定スキーマ等）は変更される可能性がある。スキルの冒頭に「本情報は開発中の段階に基づくため変更される可能性がある」という注記を追加することで、利用時の誤解を防げる。

### Dismissed

- **指摘1** (`user-invocable: false`): claude-code-guideエージェントで確認済みの有効なフロントマターフィールド。`false`に設定すると`/`メニューから非表示になり背景知識として自動参照される。他スキルで未使用なのは用途が異なるため。
- **指摘2** (`import { defineConfig } from 'vite-plus'`): 公式ドキュメント(viteplus.dev/guide/)で「vite-plus is the local package installed in each project」と明記されており、パッケージ名は`vite-plus`で正しい。
- **指摘3** (`vp test watch`): 公式ドキュメント(viteplus.dev/guide/test)から取得した形式。Vitestのサブコマンド形式を踏襲しており、`vp test run --coverage`との使い分けは意図的。
- **指摘4** (`vp env`): 公式ドキュメントで「vp env manages Node.js versions」「Vite+ will manage your global Node.js runtime and package manager」と明記。`vp env off`でオプトアウト可能。
