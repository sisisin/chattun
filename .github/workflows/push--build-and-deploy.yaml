name: Build and deploy to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'front/**'
      - 'server/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - 'mise.toml'
      - 'Dockerfile'
      - '.github/workflows/push--build-and-deploy.yaml'
      - '.github/workflows/call--deploy-service.yaml'
      - '.github/actions/**'
      - 'tools/deploy/**'
      - 'terraform/**'
      - 'vp-shared.ts'
  workflow_dispatch:

jobs:
  build:
    name: Build image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      short_sha: ${{ steps.build.outputs.short_sha }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@ba79af03959ebeac9769e648f473a284504d9193 # v2.1.10
        with:
          # ↓で設定する値を確認
          # terraform output [workload_identity_provider,deployer_service_account]
          workload_identity_provider: projects/31671566034/locations/global/workloadIdentityPools/chattun-deployment/providers/chattun-deployment-gh
          service_account: run-deployer-chattun@knowledgework-simenyan-sandbox.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: ./.github/actions/setup-gcloud

      - name: Get terraform version
        id: terraform-version
        run: |
          version=$(cat mise.toml | grep -E '^terraform' | cut -d '=' -f 2 | tr -d '[:space:]' | tr -d '"')
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Setup terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3.1.2
        with:
          terraform_version: ${{ steps.terraform-version.outputs.version }}

      - run: terraform init
        working-directory: ./terraform

      - name: Build service
        id: build
        run: |
          short_sha=$(git rev-parse --short HEAD)
          ./tools/deploy/build_image.sh --tag="${short_sha}"
          echo "short_sha=$short_sha" >> "$GITHUB_OUTPUT"

  deploy:
    name: Deploy to Cloud Run
    needs:
      - build
    uses: ./.github/workflows/call--deploy-service.yaml
    permissions:
      contents: read
      id-token: write
    secrets: inherit
    with:
      revision: ${{ needs.build.outputs.short_sha }}

  notify:
    name: Notify deploy result
    needs:
      - build
      - deploy
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Notify deploy result to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7.0.1
        with:
          script: |
            const discordWebhookUrl = process.env.DISCORD_WEBHOOK_URL;
            if (!discordWebhookUrl) {
              throw new Error('DISCORD_WEBHOOK_URL is not set');
            }
            const hasFailed = '${{ needs.build.result }}' !== 'success' || '${{ needs.deploy.result }}' !== 'success';
            const status = hasFailed ? 'failure' : 'success';
            const emoji = hasFailed ? '❌' : '✅';
            const revision = '${{ needs.build.outputs.short_sha }}';
            const commitMessage = context.payload.head_commit?.message?.split('\n')[0] ?? '';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const lines = [
              `${emoji} chattun deploy ${status}`,
              `> ${commitMessage} (${revision})`,
              `<${runUrl}>`,
            ];
            const response = await fetch(discordWebhookUrl, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({
                content: lines.join('\n'),
                username: 'chattun bot',
              }),
            });
            if (!response.ok) {
              throw new Error(`Failed to send notification: ${response.statusText}`);
            }
            core.info('Notification sent successfully');
